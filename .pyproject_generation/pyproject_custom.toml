[project]
# please adapt to package name
name = "ghga_validator"
version = "1.1.1"
description = "GHGA Validator - a short description"
dependencies = [
    "typer >= 0.9.0",
    "typer >= 0.9.0",
    "pydantic >=2, <3",
    "linkml==1.6.1",
    "linkml-runtime==1.6.0",
    "linkml-validator==0.4.5"
]

[project.urls]
# please adapt to package name
Repository = "https://github.com/ghga-de/ghga-validator"

[project.scripts]
# please adapt to package name
my-microservice = "ghga_validator.__main__:run"
